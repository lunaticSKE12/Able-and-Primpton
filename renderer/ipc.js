// const { ipcMain } = require('electron')
// const mainWindow = require('./mainWindow')

// ipcMain.on("company_id", (event, myId) =>{
//     console.log("iddddd : " + myId);
//     mainWindow.webContents.send("selected_company_id", myId);
// })

var keepID = myId

console.log("keepppp1 " + keepID)